A simple processor to translate HSBC UK PDF Statements to CSV because they don't offer this service:

##Installation:
``` bash
npm install
```

##Usage:
``` bash

node hsbc.js <path to pdf>
```

outputs to this directory as 'hsbc.csv'